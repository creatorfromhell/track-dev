<div class="switchable projects-page" { pages->projects->projects->style }>
{ form->templates->project }
{ table->projects->template }
</div>
<div class="switchable types-page" { pages->projects->types->style }>
{ form->templates->type }
{ table->types->template }
</div>